#include <bits/stdc++.h>
using namespace std;

void solve(int n, int, string s) {
}

int main() {
    int n, k;
    string s;
    cin >> n >> k >> s;
    int start = 0, end = 0;
    solve(n, k, s);
}
